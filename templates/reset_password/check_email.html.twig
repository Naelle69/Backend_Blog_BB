{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé{% endblock %}

{% block body %}
<div class="flex justify-center items-center min-h-screen px-4">
    <div class="w-full max-w-md bg-white p-6 rounded-lg shadow-md">
        <h2 class="text-2xl font-bold mb-4 text-center text-blue-700">Lien envoyé !</h2>

        <p class="text-gray-700 text-sm mb-4">
            Si un compte existe pour cette adresse email, un lien de réinitialisation vient d’être envoyé.
            Ce lien expirera dans :
            <strong class="text-gray-900">
                {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </strong>
        </p>

        <p class="text-sm text-gray-600 mb-6">
            Si vous ne recevez pas d’email, vérifiez votre dossier spam ou
            <a href="{{ path('app_forgot_password_request') }}" class="text-blue-600 hover:underline">réessayez ici</a>.
        </p>

        <div class="text-center">
            <a href="{{ path('app_login') }}"
               class="inline-block text-white bg-blue-600 hover:bg-blue-700 font-medium rounded-lg text-sm px-5 py-2.5">
               Retour à la connexion
            </a>
        </div>
    </div>
</div>
{% endblock %}
