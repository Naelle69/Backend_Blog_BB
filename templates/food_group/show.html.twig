{% extends 'base.html.twig' %}

{% block title %}FoodGroup{% endblock %}

{% block body %}
    <h1>FoodGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ food_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ food_group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_food_group_index') }}">back to list</a>

    <a href="{{ path('app_food_group_edit', {'id': food_group.id}) }}">edit</a>

    {{ include('food_group/_delete_form.html.twig') }}
{% endblock %}
