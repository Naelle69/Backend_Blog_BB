{% extends 'base.html.twig' %}

{% block title %}FoodGroup index{% endblock %}

{% block body %}
    <h1>FoodGroup index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for food_group in food_groups %}
            <tr>
                <td>{{ food_group.id }}</td>
                <td>{{ food_group.name }}</td>
                <td>
                    <a href="{{ path('app_food_group_show', {'id': food_group.id}) }}">show</a>
                    <a href="{{ path('app_food_group_edit', {'id': food_group.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_food_group_new') }}">Create new</a>
{% endblock %}
